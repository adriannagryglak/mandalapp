import React from "react";

export default function MandalaPreview(props){
    console.log(props.layers);
    return(
        <div className="mandala-preview">
        mandala preview will be here        
        </div>
    );
}